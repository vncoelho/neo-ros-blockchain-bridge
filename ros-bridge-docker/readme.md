This docker creates a simple talker with simulation of sensors data.

A screen is running and can be accesed by typing:

`screen -xS rostalker`

One can exit them by using `Ctrl-a && d` or `Ctrl-a && Ctrl-d`.
